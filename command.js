const Discord = require("discord.js");

module.exports.run = async (bot, message, arg) => {
    let sender = message.author;
    /*Comamnd Functions*/
}

module.exports.help = {
    name: "Command Name"
}